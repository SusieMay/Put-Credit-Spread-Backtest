# Minimalny zestaw zależności — warstwa danych + wizualizacje.
# Kolejne biblioteki (scipy, plotly) mogą dojść w późniejszych etapach.
pandas>=2.0
numpy>=1.24
requests>=2.31
pyyaml>=6.0
pyarrow>=14.0
pytest>=8.0
yfinance>=0.2.40
matplotlib>=3.7
